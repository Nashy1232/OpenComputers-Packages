local sides = require("sides")
local settings = {
    debug = true,
    rigs = {
        alpha = {
            mana_threshold = 0.5,
            redstone_pool_address = "3f01",
            redstone_pool_side = sides.top
        }
    },
}

return settings