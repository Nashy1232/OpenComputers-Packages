local sides = require("sides")
local settings = {
    debug = true,
    rigs = {
        alpha = {
            mana_threshold = 0.75,
            redstone_pool_address = "5ee6",
            redstone_pool_side = sides.top
        }
    },
}

return settings