local sides = require("sides")
local settings = {
    transposer = {addr = "362e", side = sides.up, component = nil},
    stages = {
        {addr = "2b82", side = sides.up, delay = 1, component = nil},
        {addr = "c0b2", side = sides.up, delay = 1, component = nil},
        {addr = "52f5", side = sides.up, delay = 1, component = nil},
        {addr = "d267", side = sides.up, delay = 1, component = nil},
        {addr = "d267", side = sides.west, delay = 1, component = nil}
    }
}

return settings